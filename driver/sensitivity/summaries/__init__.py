"""Per-study summary/plotting scripts for sensitivity analyses.

Each module reads existing results under ``results/sensitivity_studies/<study>``
and (re)generates that study's summary figures and tables -- no Gurobi, no
training. Use ``driver.sensitivity.replot`` to run them all at once.
"""
